#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int a;
    int cf=0;
    int sys=1;
    int temp=50001;
    while(cin>>a)
    {   
        if(a<temp)
        {
            temp = a;
            cf++;
        }
        
    }













    return 0;
}
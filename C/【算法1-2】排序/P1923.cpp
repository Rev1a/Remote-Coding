#include<bits/stdc++.h>
using namespace std;
int main()
{   
    int n,k;
    cin>>n>>k;
    srand(time(0));
    int pivot_index = 0 + rand() % (n + 1);
    int arr[n];
    for(int i=0;i<n;i++)
    {
        cin>>arr[i];
    }
    
}
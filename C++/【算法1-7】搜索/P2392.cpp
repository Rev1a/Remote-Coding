#include<bits/stdc++.h>
using namespace std;
long long sum = 0;
void dfs(int x)
{
    if()
    for()
}
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int nums[4];
    for(int i=0;i<4;i++)
    {
        cin>>nums[i];
    }
    
    cout<<sum<<"\n";
    return 0;
}